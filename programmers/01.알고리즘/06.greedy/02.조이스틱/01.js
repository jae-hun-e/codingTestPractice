function solution(name) {
    [...name].reduce((num, s, i) => {

        if(s === 'A'){
            if(name[i-1] !== 'A') 
        }
        const a = v.chatCodeAt(n);
        const b = a > 78 ? 91 - a : a - 65;

        if (v === "a") {
        }
    }, 0);

    return num;
}

console.log(solution("JEROEN"));
console.log(solution("JAN"));
