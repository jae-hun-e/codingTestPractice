// Node 없으면 undefuned
// left node = index * 2
// right node = index * 2 + 1
// parent node = floor(index / 2)
const tree = [undefined, 9, 3, 8, 2, 5, undefined, 7, undefined, undefined, undefined, 4];
